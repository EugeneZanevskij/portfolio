export interface Skill {
  title: string;
  image: JSX.Element;
}

export interface HistoryItem {
  role: string;
  organisation: string;
  startDate: string;
  endDate: string;
  description: string;
  image: string;
}